<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
   <div class="main w-100 min-h-screen bg-zinc-900">
    <div class="form px-10 py-10 text-white">
        <form action="/create" method="post">
            <input class="block w-full px-5 py-1 bg-zinc-800 rounded-lg" type="text" placeholder="Title" required name="title">
            <textarea class="block w-full min-h-64 px-5 mt-3 py-2 bg-zinc-800 resize-none rounded-md" required placeholder="Write your task details" name="description"></textarea>
            <input class="mt-4 font-semibold text-black bg-blue-300 
            rounded-md px-2 py-1" type="submit" value="Create Task">
        </form>
    </div>
    <div class="task flex items-center gap-5 flex-wrap p-10">
        <% if(files.length > 0)  { %>
        <% files.forEach(function(el) { %>
            <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
                <h2 class="text-white text-2xl tracking-tighter"><%= el.split(".").shift()  %></h2>
                <div class="flex w-full justify-between">
                <a class="text-blue-500 inline-block mt-3 " href="/files/<%= el.split(".").shift()%> ">Read more...</a>
                <a class="text-zinc-500 mt-3" href="/edit/<%= el.split(".").shift()%>">edit</a>
            </div>
            </div>  
        <% }); %>
        <% } else { %>
            <h3 class="text-white"> No Tasks yet ... </h3>
            <% } %>
        
    </div>
   </div>
</body>
</html>